# CI and local “Coach MVP gate” (P0-1).
# Replaces root requirements.txt for GitHub Actions — no Streamlit / full ML stack.
#
#   pip install -r requirements-ci.txt
#   set PYTHONPATH=src;src/api   (Linux: src:src/api)
#   pytest tests/mvp tests/docs_courses tests/chess_statistics -q

-r requirements-coach-mvp.txt

# SQLAlchemy postgresql:// URLs in tests still resolve to psycopg2
psycopg2-binary>=2.9,<3

pytest>=8.4,<9
requests>=2.32,<3
pandas>=2.0,<3
httpx>=0.27,<1
scikit-learn>=1.4,<2
mlflow>=2.14,<3
openpyxl>=3.1,<4
seaborn>=0.13,<1
matplotlib>=3.8,<4
imbalanced-learn>=0.12,<1
lightgbm>=4.0,<5
